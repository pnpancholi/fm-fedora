# Ghostty configuration — generated by fm-fedora
# https://ghostty.org/docs/config/reference

# Shell
shell-integration = detect

# Window
window-decorations = false
window-padding-x = 8
window-padding-y = 8

# Keybinds: managed by keybindings module (later)